class Task {
    constructor(title, desc, priority, check) {
        this.title = title;
        this.desc = desc;
        this.priority = priority;
        this.check = check;
    }
}

export default Task;